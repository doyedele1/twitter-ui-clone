import { StickySegmentDirective } from './sticky-segment.directive';

describe('StickySegmentDirective', () => {
  it('should create an instance', () => {
    const directive = new StickySegmentDirective();
    expect(directive).toBeTruthy();
  });
});
